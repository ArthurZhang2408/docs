# Conflict Demo

## Section A: Installation

Install the package with pip:

```
pip install lancedb==0.5.0
```

## Section B: Configuration

Set the connection timeout to 60 seconds.

## Section C: Usage

Connect to a local database and run a query.
